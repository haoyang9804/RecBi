short a;
int b = 1, f;
char c, e = 1;
long long d;
static short fn1() {
    unsigned g, h = 0;
    int i = 0 || d * (b | e);
    char j = a << i, l = a;
    short k;
    int m = -b;
    if (m < b) {
        k = m = b;
        g = (k || l) / (b / e);
        if (b)
            printf("g_95=%lld\n", (long long)a);
    }
    a = b = m;
    if (j || e) {
        h = g;
        i = m;
        g = j * k / (i - d);
        if (m)
            b = j && b;
        e = b * (h & d) || g;
    }
    b = i;
    char n = e || h | d;
    e = i < d & k / n;
    return f;
}

int main() {
    if (fn1())
        if (c)
lbl_2043:
            goto lbl_2043;
    return 0;
}
