int printf (const char *, ...);

unsigned int *a[1];
unsigned int *c;
unsigned int **const d = &a[0];

int b;
int e;
int f;

int main ()
{
  int g;
  unsigned int **h = &c;
  for (; f < 8; ++f)
    {
      e = h == d;
      g = e == 0 ? 0 : 1 / e;
      b |= g;
    }
  printf ("%d\n", 0);
  return 0;
}