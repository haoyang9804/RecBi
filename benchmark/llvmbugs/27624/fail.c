int printf (const char *, ...); 

int a, b[1], c, d, e, f, g, h, i, j[4843583], k, l, m, n;
unsigned o;
unsigned long long p;
char q;
short r;

void
fn1 (int p1)
{
  o = 1 & b[o ^ p1];
}

void
fn2 (long long p1)
{
  fn1 (p1 >> 56);
}

void
fn3 (int p1)
{
  int s = 0;
  r = s = -p1;
  h = s >= p1;
  if (q)
    c = 1;
}

static unsigned
fn4 ()
{
  for (i = 0;; i++)
    for (; e < 2;)
      {
    for (d = 1; d; d--)
      for (g = 0; g;)
        if (q)
          l = 0;
    for (; m; m++)
      for (k = 0; k < 10; k++)
        fn2 (j[4843582 + m * k]);
    if (q)
      break;
    n = a + 1;
    if (n)
      return 1;
    for (; f; f++)
      for (; q < 5; q++)
        ;
      }
}

static unsigned
fn5 ()
{
  fn4 ();
  return 0; 
}

int
main ()
{
  fn3 (p <= (fn5 (), 1));
  printf ("%d\n", c);
  return 0;
}
